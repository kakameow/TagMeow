<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 「所有文件访问」模式（API 30+ 的 special access）
         必须在 manifest 里声明 否则系统设置的白名单里根本不会出现本应用
         Environment.isExternalStorageManager() 永远是 false 也就是永远开不了 -->
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- API < 30 没有「所有文件访问」 只能走运行时读写权限（minSdk 26） -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- 局域网同步：裸 UDP/TCP 一样需要 INTERNET（不是只有 HTTP 才要） -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 不少机型的 Wi-Fi 驱动在省电模式下会直接丢弃广播帧 不持有 MulticastLock
         就表现为「搜索不到设备」 对应 BroadcastReceiver 里那一段 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TagMeow">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 「所有文件访问」模式下数据库里存的是绝对路径
             打开文件前必须先转成 content Uri 交给外部应用（Android 7+ 不许传 file://）
             MainActivity.providerUriOf() 用的就是这个 authority
             以前这里漏了声明 → getUriForFile 抛 IllegalArgumentException → 打开文件永远失败 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>